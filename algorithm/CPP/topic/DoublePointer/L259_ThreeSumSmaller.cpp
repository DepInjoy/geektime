/**
 * @brief   定一个长度为 n 的整数数组和一个目标值 target
 *          寻找能够使条件 nums[i] + nums[j] + nums[k] < target成立的三元组
 *          i, j, k 个数（0 <= i < j < k < n）。
 *          
 *          链接：https://leetcode-cn.com/problems/3sum-smaller
 * 
 */
#include <vector>

using namespace std;

int threeSumSmaller(vector<int>& nums, int target) {

}